package com.innovationstrategies.fip.core.domain

enum class ShardType {
    IDENTITY_CORE,
    IDENTITY_PREFS,
    BEHAVIOR_RULES,
    MEMORY_ANCHORS,
    MEMORY_EPHEMERAL,
    ACL_BASELINE,
    ACL_STATE,
    BCAL_REFINEMENTS,
    SYSTEM_META
}
